import {Engine} from '@babylonjs/core/Engines/engine';import {Scene} from '@babylonjs/core/scene';import {GameWorld} from './world';import {CorridorWorld} from './corridor';import {ArchivesWorld} from './archives';import {VaultWorld} from './vault';import {CampaignWorld} from './campaign';import {drawScene,drawTitle,type SpriteAssets} from './draw';import {drawCorridor,drawCorridorTitle} from './corridorDraw';import {drawArchives,drawArchivesTitle} from './archivesDraw';import {drawVault,drawVaultTitle} from './vaultDraw';import {drawCampaign,drawCampaignTitle} from './campaignDraw';import {AudioDirector} from './audio';import {CompletionBurst} from './effects';
export type GameHandle={scene:Scene;world:any;dispose:()=>void};
export function createGameScene(visible:HTMLCanvasElement):GameHandle{const engineCanvas=document.createElement('canvas');engineCanvas.width=2;engineCanvas.height=2;engineCanvas.style.display='none';document.body.appendChild(engineCanvas);const engine=new Engine(engineCanvas,false,{preserveDrawingBuffer:false,stencil:false});const scene=new Scene(engine);const params=new URLSearchParams(location.search);const stageNum=Number(params.get('stage')||1);const stage2=stageNum===2,stage3=stageNum===3,stage4=stageNum===4,late=stageNum>=5&&stageNum<=9;const world:any=late?new CampaignWorld(stageNum):stage4?new VaultWorld():stage3?new ArchivesWorld():stage2?new CorridorWorld():new GameWorld();if(params.has('demo'))world.start(true);const ctx=visible.getContext('2d')!;let last=performance.now();
 const sprites:SpriteAssets={finn:null,guard:null,finnWalk:[],guardWalk:[],finnJump:null,finnLand:null,finnCrouch:null,finnHide:null,guardAlert:null,props:[]};const audio=new AudioDirector();const completionFx=new CompletionBurst();let wasAlert=false,wasWin=false,wasHidden=false,wasHack=false,wasBio=false,wasOverride=false,wasUpload=false,wasEscape=false;let art={bg:null as HTMLImageElement|null,finn:null as HTMLImageElement|null,guard:null as HTMLImageElement|null};
 const load=(src:string,on:(i:HTMLImageElement)=>void)=>{const i=new Image();i.onload=()=>on(i);i.onerror=()=>console.warn(`[SIGNAL] art failed: ${src}`);i.src=src;};if(stage2){load('/generated/corridor-bg.jpg',i=>art.bg=i);}if(stage3){load('/generated/archives-bg.jpg',i=>art.bg=i);}if(stage4){load('/generated/vault-bg.jpg',i=>art.bg=i);}if(late){load('/generated/vault-bg.jpg',i=>art.bg=i);}if(stage2||stage3||stage4||late){load('/generated/finn-stage3.png',i=>art.finn=i);load('/generated/guard-stage3.png',i=>art.guard=i);}const loadSprite=(src:string,onReady:(sprite:HTMLCanvasElement)=>void)=>{const image=new Image();image.onload=()=>{const off=document.createElement('canvas');off.width=image.width;off.height=image.height;off.getContext('2d')!.drawImage(image,0,0);onReady(off);};image.src=src;};if(!stage2&&!stage3&&!stage4&&!late){loadSprite('/generated/finn-idle.png',s=>sprites.finn=s);loadSprite('/generated/guard-idle.png',s=>sprites.guard=s);loadSprite('/generated/finn-walk-0.png',s=>sprites.finnWalk[0]=s);loadSprite('/generated/finn-walk-1.png',s=>sprites.finnWalk[1]=s);loadSprite('/generated/guard-alert.png',s=>sprites.guardAlert=s);}
 const resize=()=>{const ratio=960/540;const host=visible.parentElement;const w=Math.min(host?.clientWidth??window.innerWidth,(host?.clientHeight??window.innerHeight)*ratio);visible.style.width=`${Math.max(320,w)}px`;visible.style.height=`${Math.max(180,w/ratio)}px`;};resize();window.addEventListener('resize',resize);const down=(e:KeyboardEvent)=>{audio.unlock();if(['ArrowLeft','ArrowRight','ArrowUp',' '].includes(e.key))e.preventDefault();if(e.key==='Enter'&&(world.mode==='title'||world.mode==='lose')){world.start(params.has('demo'));audio.stage();}if(e.key.toLowerCase()==='j'&&world.toggleJournal)world.toggleJournal();if(e.key.toLowerCase()==='q'&&!stage2&&!stage3&&!stage4&&!late&&world.interact){world.interact();audio.jammer();}world.press(e.key);if(e.key.toLowerCase()==='m')audio.setMuted(world.muted);};const up=(e:KeyboardEvent)=>world.release(e.key);window.addEventListener('keydown',down);window.addEventListener('keyup',up);
 engine.runRenderLoop(()=>{const now=performance.now(),dt=Math.min(.05,(now-last)/1000);last=now;const beforeX=world.player.x,beforeEvidence=world.evidenceCount?.()??world.evidence??0,beforeHack=world.hacked,beforeBio=world.biometric,beforeOverride=world.doorOverridden,beforeUpload=world.uploading,beforeEscape=world.escapeActive;world.step(dt);if(world.mode==='play'&&world.player.x!==beforeX)audio.footstep(late?'carpet':stage3||stage4?'carpet':world.player.y<350?'desk':world.player.x>700?'carpet':'tile',world.player.running);if((world.evidenceCount?.()??world.evidence??0)>beforeEvidence)audio.pickup();if(world.guard?.state==='alert'&&!wasAlert)audio.alert();if(stage3&&world.hacked&&!wasHack)audio.stage();if(stage4&&world.cameraHacked&&!wasHack)audio.stage();if(stage4&&world.biometric&&!beforeBio)audio.servo();if(stage4&&world.doorOverridden&&!beforeOverride)audio.escape();if(stage4&&world.escapeActive&&!beforeEscape)audio.stage();if(late&&world.uploading&&!beforeUpload)audio.stage();if(world.player.hidden&&!wasHidden)audio.servo();wasHidden=world.player.hidden;wasAlert=world.guard?.state==='alert';wasHack=world.hacked||world.cameraHacked;wasBio=world.biometric;wasOverride=world.doorOverridden;if(world.mode==='win'&&!wasWin){audio.escape();completionFx.trigger();wasWin=true;}completionFx.update(dt);if(world.mode!=='win')wasWin=false;if(world.mode==='title'){late?drawCampaignTitle(ctx,world):stage4?drawVaultTitle(ctx):stage3?drawArchivesTitle(ctx,world):stage2?drawCorridorTitle(ctx,world):drawTitle(ctx,world);}else if(world.mode==='play'){late?drawCampaign(ctx,world,art.finn,art.guard):stage4?drawVault(ctx,world,art.bg,art.finn,art.guard):stage3?drawArchives(ctx,world,art.bg,art.finn,art.guard):stage2?drawCorridor(ctx,world,art.bg,art.finn,art.guard):drawScene(ctx,world,sprites);}else{late?drawCampaign(ctx,world,art.finn,art.guard):stage4?drawVault(ctx,world,art.bg,art.finn,art.guard):stage3?drawArchives(ctx,world,art.bg,art.finn,art.guard):stage2?drawCorridor(ctx,world,art.bg,art.finn,art.guard):drawScene(ctx,world,sprites);ctx.fillStyle='rgba(12,14,24,.84)';ctx.fillRect(0,0,960,540);ctx.fillStyle='#d5dae8';ctx.font='28px ui-monospace,monospace';ctx.fillText(world.mode==='win'?'STAGE COMPLETE':'SIGNAL LOST',72,100);ctx.font='15px ui-monospace,monospace';ctx.fillText(world.note,72,170);ctx.fillText('ENTER  RESTART',72,222);}completionFx.draw(ctx);engine.resize();});return{scene,world,dispose:()=>{engine.stopRenderLoop();window.removeEventListener('resize',resize);window.removeEventListener('keydown',down);window.removeEventListener('keyup',up);audio.dispose();scene.dispose();engine.dispose();engineCanvas.remove();}};}
