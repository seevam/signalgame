type Particle={x:number;y:number;vx:number;vy:number;life:number;size:number;color:string};
export class CompletionBurst{private particles:Particle[]=[];trigger(){this.particles=[];const colors=['#bb9af7','#7aa2f7','#9ece6a','#e0af68','#f7768e'];for(let i=0;i<72;i++){const a=Math.random()*Math.PI*2;const speed=45+Math.random()*180;this.particles.push({x:480,y:300,vx:Math.cos(a)*speed,vy:Math.sin(a)*speed-40,life:1.2+Math.random()*.8,size:2+Math.random()*4,color:colors[i%colors.length]});}}update(dt:number){for(const p of this.particles){p.x+=p.vx*dt;p.y+=p.vy*dt;p.vy+=90*dt;p.life-=dt;}this.particles=this.particles.filter(p=>p.life>0);}draw(c:CanvasRenderingContext2D){for(const p of this.particles){c.save();c.globalAlpha=Math.max(0,Math.min(1,p.life));c.fillStyle=p.color;c.fillRect(p.x,p.y,p.size,p.size);c.restore();}}}
